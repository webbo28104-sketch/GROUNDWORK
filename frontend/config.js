// Set this to your Railway backend URL before deploying to Cloudflare Pages.
// For local development leave as empty string to use relative URLs (Flask serves both).
window.GROUNDWORK_API = window.GROUNDWORK_API || "";
